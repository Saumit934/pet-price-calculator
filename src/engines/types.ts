// TypeScript Interfaces

export interface DogCatInput {
    type: 'dog' | 'cat';
    breed: string;
    age: number;
}

export interface SmallPetInput {
    type: 'hamster' | 'guinea pig' | 'rabbit';
    breed: string;
    age: number;
}

export interface BirdInput {
    type: 'bird';
    breed: string;
    age: number;
}